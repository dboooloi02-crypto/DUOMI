# DUOMI requirements
#
# 版本来自开发机（Raspberry Pi）上 .venv 的实际安装，Python 3.13。
# 本清单只包含源码真实 import 的第三方包，不包含任何本地模型文件。
#
# 说明：DUOMI 使用 DeepSeek 作为 LLM 后端，但走 OpenAI 兼容 SDK，
#       API Key 从环境变量 DEEPSEEK_API_KEY 读取，源码内不含任何密钥。

# ── 核心（22 个当前源码模块实际 import）────────────────────
openai==3.14.1          # LLM client（DeepSeek OpenAI 兼容接口）
numpy==2.5.3            # hearing.py 音频采样处理
sherpa-onnx==1.12.39    # hearing.py 中文流式 ASR
gpiozero==2.0.1.post3   # motor_controller.py GPIO 直流电机

# ── 可选：TTS 的 Python 侧（tts.py 用 `python -m piper`）────
# tts_plus.py 走 piper-plus 的可执行文件，不需要这个包。
piper-tts==1.8.0

# ── 可选：上述包的底层依赖，显式列出以防解析冲突 ─────────────
onnxruntime==1.30.0

# ── 非 pip 的系统级依赖（需另行安装）───────────────────────
#   arecord / aplay   : ALSA 录音与播放（hearing / tts）
#   libcamera-still   : 摄像头抓帧（vision_system 通过 subprocess 调用）
#   piper-plus        : TTS 二进制，位于 .venv_piper_plus/bin/piper
#   ASR 模型          : sherpa-onnx-streaming-zipformer-small-ctc-zh-int8-2025-04-01
#   TTS 音色          : voices/*.onnx
# 以上均为大体积文件或系统包，不随仓库分发。
